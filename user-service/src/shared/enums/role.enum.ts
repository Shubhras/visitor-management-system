// These are the two roles in the system.
// Admin is assigned to React dashboard users who manage visitors.
// Resident is the mobile app user who can only create and view visitors.
export enum Role {
  ADMIN = 'admin',
  RESIDENT = 'resident',
}